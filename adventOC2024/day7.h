#pragma once

// save time includes
#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>
#include <unordered_map>

std::vector< std::pair<int, std::vector<int>>> loadEquations(std::string input_file);